#ifndef ioredirect
#define ioredirect
#include<bits/stdc++.h>
using namespace std;
// bool background;
void redirection(string,string,string,bool);
vector<string> check_redirect(vector<string>);
#endif
